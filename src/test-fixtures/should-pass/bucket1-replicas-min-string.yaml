# SPDX-License-Identifier: CC-BY-SA-4.0
# Copyright (c) 2025-2026 Kaptain contributors (Fred Cooke)
#
# Bucket 1 (minimum 0, no max). Quoted string at the lower boundary.
# Pre-section-2-migration: accepted (pattern ^[0-9]+$ matches '0').
# Post-section-2-migration: accepted (pattern unchanged).

apiVersion: kaptain.org/1.2
kind: kubernetes-app-docker-dockerfile
spec:
  main:
    generators:
      workload:
        replicas: '0'
